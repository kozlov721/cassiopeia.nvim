local Group = require('cassiopeia.utils').Group
local g = require('cassiopeia.utils').g

Group.new('makeCommands'   , g.Red        , g.Red        , g.Red        )
Group.new('makeIdent'      , g.Purple     , g.Purple     , g.Purple     )
Group.new('makeSpecTarget' , g.BlueItalic , g.BlueItalic , g.BlueItalic )
Group.new('makeTarget'     , g.Orange     , g.Orange     , g.Orange     )
